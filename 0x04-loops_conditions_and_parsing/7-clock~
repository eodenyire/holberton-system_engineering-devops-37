#!/usr/bin/env bash
#  script that displays the time for 12 hours and 59 minutes:

hours=0

while((hours<=12)); do
    echo "Hours: $hours"
    min=0
    while((min<=59)); do
        echo $min
	((min ++))
    done
    ((hours ++))
done
